name: build-api
# Stub build callback for the api component.
# Caller: cascade orchestrate-api job via uses: ./.github/workflows/build-api.yaml
on:
  workflow_call:
    inputs:
      environment:
        description: Target environment for the build
        required: false
        type: string
      sha:
        description: Commit SHA being built
        required: false
        type: string
      version:
        description: Version tag to attach to the artifact
        required: false
        type: string
      target_env:
        description: Hotfix target environment
        required: false
        type: string
    outputs:
      artifact:
        description: Identifier for the built api artifact
        value: ${{ jobs.build.outputs.artifact }}
jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      artifact: ${{ steps.build.outputs.artifact }}
    steps:
      - id: build
        shell: bash
        env:
          ENVIRONMENT: ${{ inputs.environment }}
          SHA: ${{ inputs.sha }}
          VERSION: ${{ inputs.version }}
        run: |
          echo "Building api version=${VERSION:-dev} sha=${SHA:-local} env=${ENVIRONMENT}"
          echo "artifact=api-${SHA:-local}" >> "$GITHUB_OUTPUT"
