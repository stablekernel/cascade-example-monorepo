# common

Shared library consumed by both the api service and the web app. A change here
affects both components, so both orchestrate workflows watch this path and both
component version lines advance when it moves.
